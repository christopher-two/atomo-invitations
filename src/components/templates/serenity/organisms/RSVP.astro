---
const { invitation, profile } = Astro.props;
import Footer from "../../../components/Footer.astro";
---

<section class="rsvp">
    <div class="rsvp-content" data-aos="fade-up">
        <div class="icon-container">
            <svg
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="1.5"
                stroke-linecap="round"
                stroke-linejoin="round"
                class="icon"
            >
                <path
                    d="M20.84 4.61a5.5 5.5 0 00-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 00-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 000-7.78z"
                ></path>
            </svg>
        </div>
        <h2 class="sub-heading">ASISTENCIA</h2>
        <p class="script-heading">Espero verte</p>
        <p class="rsvp-description">
            Confirma tu asistencia antes del <strong>10 de Agosto</strong> para que
            podamos organizar todo a la perfección.
        </p>

        <a
            href="https://wa.me/5211234567890?text=Hola,%20confirmo%20mi%20asistencia%20al%20evento!"
            target="_blank"
            rel="noopener noreferrer"
            class="btn-whatsapp"
            data-aos="zoom-in"
            data-aos-delay="200"
        >
            <svg
                xmlns="http://www.w3.org/2000/svg"
                width="20"
                height="20"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                class="whatsapp-icon"
                ><path
                    d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"
                ></path>
            </svg>
            CONFIRMAR POR WHATSAPP
        </a>
    </div>

    <!-- The footer stays inside the section but is placed at the absolute bottom via flex layout -->
    <div class="footer-wrapper">
        <Footer />
    </div>
</section>

<style>
    .rsvp {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        background-color: var(--color-bg);
        position: relative;
    }

    .rsvp-content {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        width: 100%;
        max-width: 600px;
        flex-grow: 1; /* Pushes footer down */
        justify-content: center;
        padding: var(--section-padding);
    }

    .icon-container {
        margin-bottom: 1.5rem;
        color: var(--color-accent);
    }

    .icon {
        width: 45px;
        height: 45px;
        opacity: 0.9;
    }

    .sub-heading {
        font-family: var(--font-sans);
        font-size: 0.9rem;
        letter-spacing: 0.4rem;
        color: var(--color-text);
        margin-bottom: 1rem;
        text-transform: uppercase;
        font-weight: 500;
        opacity: 0.6;
        margin-right: -0.4rem;
    }

    .script-heading {
        font-family: var(--font-script) !important;
        font-size: 4rem;
        color: var(--color-text);
        margin: 0 0 1.5rem 0;
        line-height: 1.2;
    }

    .rsvp-description {
        font-family: var(--font-serif);
        font-size: 1.2rem;
        color: var(--color-accent);
        line-height: 1.8;
        margin-bottom: 3.5rem;
        max-width: 400px;
    }

    .rsvp-description strong {
        color: var(--color-text);
        font-weight: 500;
    }

    .btn-whatsapp {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.8rem;
        padding: 1.2rem 2.5rem;
        background-color: transparent;
        border: 1px solid var(--color-text);
        color: var(--color-text);
        text-decoration: none;
        font-family: var(--font-sans);
        font-size: 0.85rem;
        font-weight: 500;
        letter-spacing: 0.15rem;
        transition: all 0.3s ease;
        border-radius: 6px;
    }

    .btn-whatsapp:hover {
        background-color: var(--color-text);
        color: var(--color-bg);
    }

    .whatsapp-icon {
        width: 18px;
        height: 18px;
    }

    .footer-wrapper {
        width: 100%;
    }

    @media (max-width: 480px) {
        .script-heading {
            font-size: 3.5rem;
        }
        .btn-whatsapp {
            padding: 1rem 1.5rem;
            width: 100%;
            font-size: 0.8rem;
        }
    }
</style>
