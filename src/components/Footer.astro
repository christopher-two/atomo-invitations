---
---

<footer class="shared-footer">
    <div class="footer-divider"></div>
    <span class="footer-label">ESTA PÁGINA PERTENECE A</span>
    <h2 class="footer-brand">Átomo by Override</h2>
</footer>

<style>
    .shared-footer {
        padding: 4rem 1rem;
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        width: 100%;
        background-color: transparent;
    }

    .footer-divider {
        width: 1px;
        height: 40px;
        background: linear-gradient(
            to bottom,
            var(--color-accent),
            transparent
        );
        margin-bottom: 2rem;
        opacity: 0.6;
    }

    .footer-label {
        font-family: var(--font-sans);
        font-size: 0.65rem;
        letter-spacing: 0.25rem;
        text-indent: 0.25rem;
        color: var(--color-accent);
        text-transform: uppercase;
        margin-bottom: 1rem;
        opacity: 0.8;
    }

    .footer-brand {
        font-family: var(--font-serif);
        font-style: italic;
        font-size: 1.4rem;
        color: var(--color-text);
        margin: 0;
        font-weight: 300;
        letter-spacing: 0.05rem;
    }

    @media (max-width: 480px) {
        .footer-brand {
            font-size: 1.2rem;
        }
    }
</style>
